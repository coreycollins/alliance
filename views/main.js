$(function ($, _, Backbone) {

  var MainView = Backbone.View.extend({

    el: '#main',

    events: {
    },

    // Re-render the titles of the todo item.
    render: function() {

    }

  });

  // Create the app
  var mainView = new MainView();
  
}(jQuery, _, Backbone));